<%- include('partials/header', { title: 'Success | Encore', activePage: null }) %>

    <div class="container py-5 d-flex justify-content-center">
      <div class="card card-elevated p-4 text-center" style="max-width: 480px; width: 100%">
        <div class="display-5 text-success mb-3">✓</div>
        <h3 class="fw-bold mb-2"><%= message %></h3>
        <p class="text-muted">You can head back to the dashboard or keep exploring.</p>
        <div class="d-flex justify-content-center gap-2 mt-3">
          <a class="btn btn-success" href="/dashboard">Dashboard</a>
          <a class="btn btn-outline-success" href="/directory">Directory</a>
        </div>
      </div>
    </div>

<%- include('partials/footer') %>
