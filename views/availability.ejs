<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Availability | Encore</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: #f8f9fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }
      .navbar {
        background-color: #198754;
      }
      .navbar-brand,
      .nav-link {
        color: #fff !important;
        font-weight: 600;
      }
      .card {
        border: none;
        border-radius: 1rem;
        box-shadow: 0 0.25rem 0.85rem rgba(0, 0, 0, 0.08);
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand" href="/dashboard">Encore</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/profile">Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/directory">Directory</a>
            </li>
            <li class="nav-item ms-lg-3">
              <a class="btn btn-light text-success" href="/logout">Logout</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <% if (flashMessage) { %>
          <div class="alert alert-success"><%= flashMessage %></div>
          <% } %>

          <div class="card p-4">
            <h3 class="fw-bold text-success mb-3">Set your availability</h3>
            <p class="text-muted">
              Toggle whether you are currently open for bookings. Organizers will see this status in the directory and on your profile.
            </p>

            <form action="/availability" method="POST" class="mt-3">
              <div class="form-check mb-2">
                <input
                  class="form-check-input"
                  type="radio"
                  name="availability"
                  id="availableYes"
                  value="Y"
                  <%= availability === 'Y' ? 'checked' : '' %>
                />
                <label class="form-check-label" for="availableYes">I am available for bookings</label>
              </div>
              <div class="form-check mb-4">
                <input
                  class="form-check-input"
                  type="radio"
                  name="availability"
                  id="availableNo"
                  value="N"
                  <%= availability !== 'Y' ? 'checked' : '' %>
                />
                <label class="form-check-label" for="availableNo">I am not available right now</label>
              </div>

              <div class="d-flex gap-2">
                <button class="btn btn-success" type="submit">Save availability</button>
                <a class="btn btn-outline-secondary" href="/dashboard">Cancel</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
